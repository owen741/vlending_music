# 배포 후 이전 디자인이 나오는 문제 해결

## 🔍 문제 원인

배포를 해도 이전 디자인이 나오는 주요 원인:

1. **빌드 캐시 문제**: Vite가 이전 빌드 결과를 캐시하고 있음
2. **GitHub Pages 캐시**: GitHub Pages가 이전 버전을 캐시하고 있음
3. **브라우저 캐시**: 브라우저가 이전 버전을 캐시하고 있음
4. **빌드가 제대로 되지 않음**: 최신 코드로 빌드되지 않음

---

## 🛠️ 해결 방법

### 방법 1: 완전히 새로운 빌드 및 배포 (권장)

터미널에서 다음 명령어를 실행하세요:

```bash
cd /Users/owen/Documents/GitHub/vlending_music

# 1. 이전 빌드 및 캐시 삭제
rm -rf dist node_modules/.vite .vite

# 2. 새로운 빌드
npm run build

# 3. 배포 (타임스탬프 포함)
npx gh-pages -d dist --message "Deploy: $(date +%Y-%m-%d\ %H:%M:%S) - 캐시 무효화"
```

또는 배포 스크립트 사용:

```bash
chmod +x deploy-fresh.sh
./deploy-fresh.sh
```

---

### 방법 2: Vite 빌드 캐시 무효화

```bash
cd /Users/owen/Documents/GitHub/vlending_music

# Vite 캐시 삭제
rm -rf node_modules/.vite .vite dist

# 강제 재빌드
npm run build -- --force

# 배포
npx gh-pages -d dist
```

---

### 방법 3: GitHub Pages 캐시 무효화

배포 후 HTML 파일에 캐시 무효화 파라미터 추가:

1. `index.html`에 버전 파라미터 추가
2. 또는 배포 메시지에 타임스탬프 포함

```bash
npx gh-pages -d dist --message "Deploy: $(date +%Y-%m-%d\ %H:%M:%S)"
```

---

### 방법 4: 브라우저 캐시 무효화

배포 후 브라우저에서:

1. **하드 리프레시**:
   - Mac: `Cmd + Shift + R`
   - Windows: `Ctrl + Shift + R`
   - Linux: `Ctrl + Shift + R`

2. **시크릿 모드에서 확인**:
   - Mac: `Cmd + Shift + N`
   - Windows: `Ctrl + Shift + N`

3. **브라우저 캐시 삭제**:
   - Chrome: 설정 → 개인정보 및 보안 → 인터넷 사용 기록 삭제
   - Safari: 개발자 메뉴 → 캐시 비우기

---

## 📋 완전한 해결 체크리스트

### Step 1: 빌드 캐시 삭제
```bash
cd /Users/owen/Documents/GitHub/vlending_music
rm -rf dist node_modules/.vite .vite
```

### Step 2: 새로운 빌드
```bash
npm run build
```

### Step 3: 빌드 확인
```bash
# dist 폴더의 파일 수정 시간 확인
ls -la dist/

# index.html의 내용 확인 (최신 코드인지 확인)
head -20 dist/index.html
```

### Step 4: 배포
```bash
npx gh-pages -d dist --message "Deploy: $(date +%Y-%m-%d\ %H:%M:%S)"
```

### Step 5: 확인
1. 배포 후 5-10분 대기
2. 하드 리프레시 실행
3. 시크릿 모드에서 확인
4. GitHub 저장소의 gh-pages 브랜치 확인

---

## 🔧 Vite 빌드 설정 최적화

`vite.config.ts`에 캐시 무효화 설정 추가:

```typescript
export default defineConfig({
  base: '/vlending_music/',
  plugins: [react()],
  build: {
    target: 'esnext',
    outDir: 'dist',
    // 캐시 무효화를 위한 해시 추가
    rollupOptions: {
      output: {
        // 파일명에 해시 추가하여 캐시 무효화
        entryFileNames: 'assets/[name].[hash].js',
        chunkFileNames: 'assets/[name].[hash].js',
        assetFileNames: 'assets/[name].[hash].[ext]',
      },
    },
  },
  // ...
});
```

이미 Vite는 기본적으로 파일명에 해시를 추가하므로, 문제는 빌드 자체가 아닐 수 있습니다.

---

## 💡 추가 팁

### 1. 배포 전 확인
```bash
# 개발 서버에서 최신 코드 확인
npm run dev
```

### 2. 빌드 파일 확인
```bash
# dist 폴더의 파일 확인
ls -la dist/

# index.html 확인
cat dist/index.html
```

### 3. GitHub Pages 설정 확인
- GitHub 저장소 → Settings → Pages
- Source: `gh-pages` 브랜치 확인
- Custom domain이 있다면 확인

### 4. 배포 로그 확인
```bash
# 배포 후 GitHub Actions 로그 확인 (있다면)
# 또는 gh-pages 브랜치의 최근 커밋 확인
```

---

## 🚨 여전히 문제가 있다면

1. **GitHub 저장소 확인**:
   - `gh-pages` 브랜치의 최근 커밋 시간 확인
   - 파일 내용이 최신인지 확인

2. **로컬 빌드 확인**:
   ```bash
   npm run build
   npm run dev
   # 로컬에서 최신 디자인이 보이는지 확인
   ```

3. **CDN 캐시 확인**:
   - GitHub Pages는 CDN을 사용하므로 전 세계적으로 캐시가 분산됨
   - 다른 네트워크에서 확인해보기
   - VPN을 사용하여 다른 지역에서 확인

4. **파일 수정 시간 확인**:
   ```bash
   # dist 폴더의 파일 수정 시간 확인
   ls -la dist/
   # 최근에 수정된 파일인지 확인
   ```

---

## 📞 문제 해결이 안 되면

다음 정보를 확인해주세요:

1. **배포 시간**: 언제 배포했나요?
2. **확인 방법**: 어떤 브라우저에서 확인했나요?
3. **하드 리프레시**: 하드 리프레시를 시도했나요?
4. **로컬 확인**: `npm run dev`로 로컬에서 최신 디자인이 보이나요?

